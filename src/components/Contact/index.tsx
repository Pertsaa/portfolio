import { FC } from 'react';

import { Section, Container, Title } from '../shared-styles';

const Contact: FC = () => {
  return (
    <Section>
      <Container>
        <Title>Contact</Title>
      </Container>
    </Section>
  );
};

export default Contact;
